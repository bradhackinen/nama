from setuptools import setup

setup(
    name='nama',
    version='0.0.1',
    packages=['nama'],
    install_requires=[],
    url='https://github.com/bradhackinen/nama',
    description='A NAme MAtching tool' 
)